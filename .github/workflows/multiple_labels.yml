
on:
  workflow_dispatch:
    inputs:
      couple:
      checks:

env: 
  key: value
  
jobs:
  test:
    name: Test
    runs-on: "ubuntu-latest"
    
    steps:
      - name: What is insert?
        run: |
          echo "{{ toJson(insert) }}"
