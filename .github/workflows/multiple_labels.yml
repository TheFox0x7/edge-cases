
on:
  Workflow_dispatch:
    inputs:
      couple:
      checks:

enV: 
  key: value
  
jobs:
  test:
    name: Test
    runs-on: "ubuntu-latest"
    
    steps:
      - run: |
          echo "${{ env.key }}"
